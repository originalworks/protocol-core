type NewBlob @entity(immutable: false) {
  id: String!
  blobSubmitters: [Bytes!]!
}

type TrackProcessedWithSubmitter @entity(immutable: false) {
  id: String!
  isrc: String!
  submitters: [Bytes!]!
}
